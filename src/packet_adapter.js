/**
 * @class
 * This documents how incoming XML stanzas should be wrapped
 * to be ingested by OX properly. This is a stub, and creates a
 * specification for you (the developer) to interface with OX.
 *
 * @name OX.PacketAdapter
 */

/**
 * @name OX.PacketAdapter#getFrom
 * @function
 * @description
 * Get the JID that the stanza was sent from.
 * @returns {String} The JID of the sender.
 */

/**
 * @name OX.PacketAdapter#getTo
 * @function
 * @description
 * Get the JID that the stanza was sent to.
 * @returns {String} The target JID.
 */

/**
 * @name OX.PacketAdapter#getType
 * @function
 * @description
 * Get the type of the packet (the type attribute in the XML schema).
 * @returns {String} The type of the stanza.
 */

/**
 * @name OX.PacketAdapter#getNode
 * @function
 * @description
 * Get the root node of the XML stanza.
 * @returns {Element} The root element of the XML stanza.
 */
