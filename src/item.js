/**
 * Base Item object.
 * @namespace
 * @extends OX.Base
 */
OX.Item = OX.Base.extend(/** @lends OX.Item# */{
  /**
   * The URI of this item as an OX.URI.Base object.
   * @type OX.URI.Base
   */
  uri: null
});
